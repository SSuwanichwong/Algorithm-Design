#include <bits/stdc++.h>

using namespace std;

const int x = 1000003;

int main() {
    int n, k; // n pieces size of set S
    cin >> n >> k;

    vector<int> s(k);
    for (int i=0; i<k; i++) cin >> s[i];


}