#include <bits/stdc++.h>

using namespace std;

int main() {
    int n; //number of billboard
    cin >> n;
    vector<int> billboards(n+1);
    for (int i=1; i<n+1; i++) cin >> billboards[i];

    
}